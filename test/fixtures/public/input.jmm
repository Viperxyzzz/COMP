import a1.blue;
class c1 extends otherclass {
    public int f () {
        new int [a + 4 * b];
        new a();
        b = a[1] + a[2];
        if (a && b) {
            amples_in_circle = samples_in_circle + 1;
        } else if (a > b) {
            a = 2;
        }
        else{
        }
        return -(a+b);
    }
}