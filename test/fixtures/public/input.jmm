import a1.blue.red.white;
import a2;
class c1 extends otherclass {
    public f f (int a, int c) {
       a = 1 + 2;
       while (a < 3*4) {
       			if ((a/4) < b*5-a) {
       				samples_in_circle = samples_in_circle + 1;
       			} else if (a > b) {
       			    a = 2;
       			}
       			else{
       			}
       			samples_so_far = samples_so_far + 1;
       		}
       return -(a+b);
    }
}