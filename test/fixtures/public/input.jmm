import a1.blue;
import otherclass;
class c1 extends otherclass {
    int one;
    int two;
    int[] three;
    public static void main(String[] args) {

    		ioPlus.printHelloWorld(one, two);
    		ioPlus.length;
    }
    public int f (int a, int b) {
        int[] test;
        int a;

        if (a && !a && b)
           winner = 0;
        else{}
        return -(a+b);
    }
}